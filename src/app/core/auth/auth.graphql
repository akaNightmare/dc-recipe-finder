mutation SignIn($auth: AuthInput!) {
  signIn(auth: $auth) {
    access_token
    user {
      id
      login
    }
  }
}

mutation CreateUser($auth: AuthInput!) {
  createUser(auth: $auth) {
    id
    login
  }
}
